# URL base da API exposta pelo Next (proxy reescreve para o tenant correto)
NEXT_PUBLIC_API_URL=/api

# URL real do backend Django (usada pelo rewrite do Next no server-side)
# Deve apontar para o domínio do tenant (ex: demo.localhost:8000)
NEXT_PUBLIC_API_PROXY_URL=http://demo.localhost:8000
